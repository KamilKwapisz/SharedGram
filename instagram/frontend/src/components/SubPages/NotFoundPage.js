import React from "react";

function NotFoundPage(){

    return(
        <div>
            <p>
                Subpage you are trying to access doesn't exist.
            </p>
        </div>
    );
}

export default NotFoundPage;
